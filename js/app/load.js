define(
    function(){
        var Load = {},
            state;

        Load.main = function( collection ){
            state = collection;
        };

        Load.backgroundView = function(){
        };

        Load.backgroundScrollBuffer = function(){

        };

        Load.overlay = function(){

        };
    }
)
